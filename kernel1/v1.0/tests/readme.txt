These are test routines

To run a test, use the ".include" directive to include a test file into "kernel.s", then add the instruction "b _teststart" right after the label "_sysmain".
Only one test file may be included at a time.

